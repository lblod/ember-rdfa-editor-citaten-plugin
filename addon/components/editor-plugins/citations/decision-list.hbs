{{#if @decisions.length}}
  <AuList @direction="vertical" @divider={{true}}>
    {{#each @decisions as |decision|}}
      <AuList::Item>
        <EditorPlugins::Citations::DecisionPreview
          @decision={{decision}}
          @onCitationInsert={{fn @onCitationInsert decision}}
          @onCitationDetails={{fn @onCitationDetails decision}}
          @fullSize={{@fullSize}} />
      </AuList::Item>
    {{/each}}
  </AuList>
{{else}}
  <EditorPlugins::Helpers::AlertNoItems @fullSize={{@fullSize}} class={{if @fullSize "au-u-margin"}}/>
{{/if}}
