<AuAlert
  @alertTitle="{{t "alert.no-results"}}"
  @alertSkin="warning"
  @alertIcon="cross"
  @alertSize="{{if @fullSize "default" "small"}}"
  @closable={{false}}
  class="{{if (not @fullSize) "au-u-margin-top-small"}}"
  ...attributes>
</AuAlert>

