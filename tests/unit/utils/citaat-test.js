import citaat from 'dummy/utils/citaat';
import { module, test } from 'qunit';

module('Unit | Utility | citaat');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = citaat();
  assert.ok(result);
});
