import legislationTypes from 'dummy/utils/legislation-types';
import { module, test } from 'qunit';

module('Unit | Utility | legislation-types', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = legislationTypes();
    assert.ok(result);
  });
});
